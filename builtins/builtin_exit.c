#include "minishell.h"

int		builtin_exit(t_block *block)
{
	//if (block->argc == )
	//else if (block->argc == )
	(void)block;
	exit(0);
	return (0);
}

//https://tldp.org/LDP/abs/html/exitcodes.html
